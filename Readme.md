create a UI for a chat app, with a demo auth login adn signup system( the user flow would be, a user creates the account by adding credentials and then teh information will be sent to the home page via state management redux, and the home page checks if their is any login info.. the login page also works by that , firstly user goes to suignup then to login , after their information is added to localhost for comparing, adn the home paeg should be having options like view profile and chat... on the chat page, ther should be something like a general chat, where the user  can type and send messages, and the messages will be get from baclend api provided in the readme .md file referenced, and the users should be able to send it .